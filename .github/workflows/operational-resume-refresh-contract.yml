name: Operational Resume Refresh Contract

on:
  pull_request:
    branches: [main]
    paths:
      - 'index.html'
      - 'operational_resume_refresh-1.0.0.js'
      - 'agenda_lifecycle-1.0.0.js'
      - 'agenda-1.20.0.js'
      - 'home_fisioterapeuta_util-1.24.0.js'
      - 'tests/operational_resume_refresh.contract.js'
      - '.github/workflows/operational-resume-refresh-contract.yml'
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'operational_resume_refresh-1.0.0.js'
      - 'agenda_lifecycle-1.0.0.js'
      - 'agenda-1.20.0.js'
      - 'home_fisioterapeuta_util-1.24.0.js'
      - 'tests/operational_resume_refresh.contract.js'
      - '.github/workflows/operational-resume-refresh-contract.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: JavaScript syntax
        run: |
          node --check operational_resume_refresh-1.0.0.js
          node --check tests/operational_resume_refresh.contract.js
      - name: Resume refresh contract
        run: node tests/operational_resume_refresh.contract.js
      - name: Diff hygiene
        run: git diff --check
